const inicio = ( req, res ) => {
    res.render('inicio', {
        pagina: 'Inicio'
    })
}

const categorias = ( req, res ) => {
    
}

const noEncontrado = ( req, res ) => {
    
}

const buscador = ( req, res ) => {
    
}

export{
    inicio,
    categorias,
    noEncontrado,
    buscador
}